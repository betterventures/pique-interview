<div id="app">
  <% @applicants.each do |k,v| %>
    <% v.each do |a| %>
      <div>
        <br>
        <%= a %>
      </div>
    <% end %>
    <div>
      <%= @user.to_json %>
    </div>
  <% end %>

  <%= react_component('Provider/index', {applicants: @applicants, user: @user}) %>
</div>
