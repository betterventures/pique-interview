<div class="nav">
	<% if provider_signed_in? %>
		<li>
		<%= link_to('Logout', destroy_provider_session_path, :method => :delete) %>
		</li>
	<% else %>
		<li>
		<%= link_to('Login', new_provider_session_path)  %>
		</li>
	<% end %>

	<% if provider_signed_in? %>
		<li>
		<%= link_to('Edit registration', edit_provider_registration_path) %>
		</li>
	<% else %>
		<li>
		<%= link_to('Register', new_provider_registration_path)  %>
		</li>
	<% end %>
</div>
